import { icons3d } from '../../assets/icons-3d'

// Só os padrões já publicados: os "em breve" usam o ícone de ampulheta.
export const patternIcons: Record<string, string> = {
  'feature-based': icons3d.puzzlePiece,
  'atomic-design': icons3d.atomSymbol,
  'layered-clean': icons3d.shield,
}
